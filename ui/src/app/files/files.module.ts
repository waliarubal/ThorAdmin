import { NgModule } from '@angular/core';
import { FilesComponent } from './files.component';
import { FilesRoutingModule } from './files.routing';

@NgModule({
  declarations: [FilesComponent],
  imports: [FilesRoutingModule],
  providers: [],
})
export class FilesModule {}
